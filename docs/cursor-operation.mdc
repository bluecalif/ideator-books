---
alwaysApply: true
description: Cursor 운영 환경 가이드 - Windows PowerShell 및 터미널 관리 규칙
---

# Cursor 운영 환경 가이드

> **중요**: 이 프로젝트는 Windows PowerShell 환경에서 실행됩니다. Bash 문법과 다른 PowerShell 고유 문법을 사용해야 합니다.

## 목차
1. [Windows PowerShell 명령어 가이드](#1-windows-powershell-명령어-가이드)
2. [터미널 홀드 문제 해결](#2-터미널-홀드-문제-해결)
3. [커서룰 작성 가이드라인](#3-커서룰-작성-가이드라인)
4. [환경변수 파일 관리](#4-환경변수-파일-관리)

---

## 1. Windows PowerShell 명령어 가이드

### 1.1 기본 문법 차이점

**PowerShell 5.1 (기본 Windows 환경)**
```powershell
# ✅ 올바른 문법
cd C:\Projects\myapp; pytest tests/
command1; command2

# ❌ Bash 문법 (작동 안 함)
cd C:\Projects\myapp && pytest tests/
export API_KEY=xxx
```

**PowerShell 7+ (PowerShell Core)**
```powershell
# ✅ PowerShell 7+에서만 작동
cd C:\Projects\myapp && pytest tests/
```

### 1.2 자주 사용하는 명령어

**디렉토리 작업**
```powershell
# 디렉토리 생성 (-Force: 부모 디렉토리 자동 생성)
New-Item -ItemType Directory -Path "backend\api\models" -Force
mkdir backend\new_module

# 디렉토리 이동/확인
cd C:\Projects
pwd  # 현재 위치
```

**파일 작업**
```powershell
# 파일 읽기/복사/삭제
Get-Content file.txt
Copy-Item source.txt destination.txt
Remove-Item file.txt
```

**Python 실행**
```powershell
# Python 스크립트 실행
python backend/tests/test_pdf_parsing.py
python -m pytest backend/tests/

# 환경 변수 설정 후 실행
$env:LOG_LEVEL = "DEBUG"; python script.py

# Python -c 명령어 (작은따옴표 사용)
python -c 'print("Hello World")'
```

**프로젝트 서버 실행 (중요)**
```powershell
# ⚠️ 항상 프로젝트 루트에서 실행
# 서버 실행 전 .env 파일 확인 필수

# 프론트엔드 (Next.js)
cd frontend; npm run dev

# 백엔드 (FastAPI) - PYTHONPATH 설정 필수
$env:PYTHONPATH = "C:\Projects\vibe-coding\books-lesson"
cd backend; python -m uvicorn api.main:app --reload --port 8000

# 또는 한 줄로
cd C:\Projects\vibe-coding\books-lesson; $env:PYTHONPATH = "C:\Projects\vibe-coding\books-lesson"; cd backend; python -m uvicorn api.main:app --reload --port 8000
```

**환경 변수**
```powershell
# 세션 범위 설정
$env:API_KEY = "your_key_here"
$env:PYTHONPATH = "C:\Projects\vibe-coding\books-lesson"

# 환경 변수 확인
echo $env:API_KEY
Get-ChildItem Env:

# ❌ 중요: .env 파일은 자동으로 로드되지 않음
# 백엔드 서버 실행 시 필수 환경변수:
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_KEY
# - OPENAI_API_KEY
# - UPSTAGE_API_KEY
# - PYTHONPATH (프로젝트 루트 경로)
```

**npm 명령어**
```powershell
npm install
npm run dev
npm run build
npm run lint
npx tsc --noEmit
```

### 1.3 출력 제한 및 필터링

**출력 줄 수 제한 (head/tail 대체)**
```powershell
# ❌ Bash 스타일 (작동 안 함)
git diff file.py | head -50

# ✅ PowerShell 스타일
git diff file.py | Select-Object -First 50
git log | Select-Object -Last 20
```

**출력 필터링**
```powershell
# 특정 패턴 포함/제외
git status | Select-String "modified"
git status | Select-String -NotMatch "node_modules"
```

### 1.4 인코딩 문제 해결

**문제**: Windows PowerShell의 기본 인코딩(cp949)은 유니코드 이모지를 지원하지 않습니다.

```python
# ❌ PowerShell에서 오류 발생
print("✓ Test passed")
print("✅ Success")

# ✅ 올바른 방법
print("[OK] Test passed")
print("[PASS] Success")
print("[FAIL] Failed")
```

**Python 코드 작성 가이드라인**:
- ✓, ✅, ❌ 등 이모지 사용 금지
- `[OK]`, `[PASS]`, `[FAIL]` 사용
- 영문 텍스트: `Success`, `Failed`, `Completed`

---

## 2. 터미널 홀드 문제 해결

### 2.1 문제 상황
터미널이 특정 프로세스(예: Next.js 개발 서버)에 바인딩되어 명령어 입력이 불가능한 상태

### 2.2 즉시 해결 방법

**1. 프로세스 강제 종료**
```powershell
# Ctrl+C로 현재 프로세스 종료
# 또는
taskkill /F /IM node.exe
```

**2. 포트 점유 프로세스 확인 및 종료**
```powershell
# 포트 사용 프로세스 확인
netstat -ano | findstr :3000
netstat -ano | findstr :3001

# PID로 프로세스 종료
taskkill /F /PID [PID번호]
```

**3. 새 터미널 세션 시작**
- Cursor에서 새 터미널 탭 열기
- 또는 기존 터미널 재시작

### 2.3 근본적 해결 방법

**백그라운드 프로세스 관리**
```powershell
# 백그라운드에서 실행 (PowerShell 7+)
Start-Process npm -ArgumentList "run", "dev" -WindowStyle Hidden

# 프로세스 모니터링
Get-Process | Where-Object {$_.ProcessName -eq "node"}
```

### 2.4 문제 발생 시 대응 순서
1. **Ctrl+C로 프로세스 종료**
2. **포트 점유 프로세스 확인 및 종료**
3. **새 터미널 세션 시작**
4. **명령어 재실행**

---

## 3. 커서룰 작성 가이드라인

### 3.1 핵심 원칙

**1. 추측 금지 - 실제 코드 확인 필수**
```
실제 코드 확인 → 입출력 형식 파악 → 정확한 예제 작성 → 서비스 구현
```

**2. 기존 시스템 이해 - 최적화 무효화 방지**
- 새 기능 추가 시 기존 최적화에 미치는 영향 고려
- 캐시, 성능 최적화 등 기존 시스템의 전제 조건 파악

**3. Bottom-up 검증 - 가정하지 말고 확인**
- 실제 코드: `lib/**/*.ts`, `app/api/**/*.ts`
- 타입 정의: `types/index.ts`
- API 응답: 브라우저 Network 탭, `/api/collect` 실행 결과

### 3. 커서룰 작성 체크리스트

**새로운 통합 레이어 작성 시**:
- [ ] 기존 모듈의 실제 코드를 읽었는가?
- [ ] 테스트 파일에서 사용 방식을 확인했는가?
- [ ] 캐시 등 기존 최적화 시스템을 파악했는가?
- [ ] 예제 코드에 실제 데이터 구조를 반영했는가?

**커서룰 예제 코드 작성 시**:
- [ ] 추상적 예제가 아닌 실제 코드 복사인가?
- [ ] 주석에 "참고용", "실제와 다를 수 있음" 명시했는가?

### 3.3 좋은 커서룰 예시

**❌ 나쁜 예 (추측 기반)**:
```typescript
const videos = response.items;  // 추측
```

**✅ 좋은 예 (사실 기반)**:
```typescript
// 실제 YouTube API 응답 기반
// GET /api/collect 응답 형식:
// { success: true, data: { videos: [...] } }
const videos = response.data.videos;  // ✅ 정확
```

### 3.4 통합 작업 시 필수 확인사항

1. **입력 형식 확인**: 함수가 기대하는 입력 파라미터
2. **출력 형식 확인**: 함수의 반환값 타입
3. **부수 효과 확인**: 데이터베이스 변경, 캐시 영향
4. **의존성 확인**: 환경 변수, 외부 API

### 3.5 오류 진단 가이드라인

**HTTP 상태 코드 기반 진단**:
- Status 200: 연결 문제 없음 (코드 레벨 오류 확인)
- Status 500: 내부 서버 오류 (JavaScript/TypeScript 오류)
- Status 502/503: 인프라 레벨 연결 문제

**자주 발생하는 오류 패턴**:
- `ReferenceError: XXX is not defined`: 상수/변수 정의 문제
- `TypeError: XXX is not a function`: 함수 구현 누락
- `API_CONFIG is not defined`: import/export 문제

---

## 4. 환경변수 파일 관리

### 4.1 문제 상황
프로젝트는 `.env` 또는 `.env.local` 파일을 사용할 수 있으며, AI 어시스턴트가 이를 인식하지 못하는 경우가 발생합니다.

### 4.2 원인 분석
1. **숨김 파일 속성**: `.env` / `.env.local` 파일이 숨김 속성으로 설정되어 있음
2. **도구 한계**: `glob_file_search`, `read_file` 도구가 숨김 파일을 처리하지 못함
3. **권한 문제**: 파일 읽기 권한 부족

### 4.3 해결 방법

**파일 존재 확인 (PowerShell -Force 옵션 필수)**
```powershell
# ✅ 올바른 방법 - 숨김 파일 포함 검색
Get-ChildItem -Name "*.env*" -Force

# ✅ 파일 내용 확인 (프로젝트 루트)
Get-Content .env
Get-Content .env.example

# ✅ 파일 내용 확인 (프론트엔드)
Get-Content frontend\.env.local

# ❌ 잘못된 방법 - 숨김 파일 미포함
dir *.env*
ls .env*
```

**환경변수 파일 위치**
- **백엔드**: `C:\Projects\vibe-coding\books-lesson\.env` (프로젝트 루트)
- **프론트엔드**: `C:\Projects\vibe-coding\books-lesson\frontend\.env.local`

**환경변수 파일 생성 예시**
```bash
# .env (백엔드 환경변수 - 프로젝트 루트)
UPSTAGE_API_KEY=your_upstage_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_KEY=your_supabase_service_key_here

# frontend\.env.local (프론트엔드 환경변수)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

### 4.4 AI 어시스턴트 가이드

**환경변수 파일 확인 절차 (우선순위 순)**:
1. **PowerShell 명령어 사용 (권장)**: `Get-ChildItem -Name "*.env*" -Force`
2. **터미널 명령어 사용**: `dir /a *.env*` (Windows) 또는 `ls -la .env*` (Linux/Mac)
3. **도구 사용 (보조)**: `glob_file_search`, `list_dir` (숨김 파일 인식 한계 있음)

**환경변수 파일 위치 확인**:
```powershell
# 프로젝트 루트 .env 파일 확인
Test-Path .env
Get-Content .env

# 프론트엔드 .env.local 파일 확인
Test-Path frontend\.env.local
Get-Content frontend\.env.local
```

**실제 사례: 숨김 파일 인식 실패**
```powershell
# ❌ 실패 사례
glob_file_search(".env*")  # 결과: 0개 파일
read_file(".env")          # 오류: Could not find file

# ✅ 성공 사례
Get-ChildItem -Name "*.env*" -Force  # 결과: .env.example, .env
Get-Content .env                      # 파일 내용 정상 출력
```

**백엔드 환경변수 검증 (Python)**
```python
# backend/config/settings.py
from pydantic_settings import BaseSettings
from typing import Optional

class Settings(BaseSettings):
    upstage_api_key: str
    openai_api_key: str
    supabase_url: Optional[str] = None
    supabase_anon_key: Optional[str] = None
    supabase_service_key: Optional[str] = None
    
    class Config:
        env_file = ".env"  # 프로젝트 루트의 .env 파일 읽기
        env_file_encoding = "utf-8"
        extra = "ignore"  # .env에 정의되지 않은 환경 변수 무시
```

**프론트엔드 환경변수 검증 (TypeScript)**
```typescript
export function validateEnvVars(): boolean {
  const requiredVars = [
    'NEXT_PUBLIC_API_URL',
    'NEXT_PUBLIC_SUPABASE_URL',
    'NEXT_PUBLIC_SUPABASE_ANON_KEY'
  ];

  const missingVars = requiredVars.filter(varName => !process.env[varName]);
  
  if (missingVars.length > 0) {
    console.error('누락된 환경변수:', missingVars);
    return false;
  }
  
  return true;
}
```

### 4.5 Next.js 환경변수 규칙

**클라이언트 사이드**: `NEXT_PUBLIC_` 접두사 필수, 빌드 타임에 번들에 포함
**서버 사이드**: 접두사 없이 사용 가능, 런타임에만 접근 가능

```typescript
// 클라이언트에서 사용 가능
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;

// 서버에서만 사용 가능
const apiKey = process.env.YOUTUBE_API_KEY;
```

---

## 5. 실제 DB 연결 통합 테스트 작성

### 5.1 Mock vs 실제 통합 테스트

**문제**: Mock 테스트만으로는 실제 DB 연결 및 데이터 흐름 검증 불가

**해결**: 실제 Supabase 연결 통합 테스트 추가

```python
# backend/tests/test_integration_quiz.py
"""
실제 Supabase 연결 및 데이터로 QuizGenerator 검증
"""

import pytest
from backend.api.services.quiz_generator import QuizGenerator
from backend.api.supabase_client import get_supabase

@pytest.fixture
def supabase_client():
    """실제 Supabase 클라이언트"""
    try:
        return get_supabase()
    except Exception as e:
        pytest.skip(f"Supabase 연결 실패: {e}")

def test_quiz_generator_with_real_data(supabase_client):
    """실제 데이터로 퀴즈 생성 테스트"""
    generator = QuizGenerator(supabase_client)
    
    # 기존 데이터 사용 (Phase 1 마이그레이션)
    book_result = supabase_client.table("books").select("id, structure_data").limit(1).execute()
    assert len(book_result.data) > 0
    
    logger.info("[OK] QuizGenerator 실제 DB 연결 성공")
```

### 5.2 테스트 실행 방법

**PowerShell에서 pytest 실행**:
```powershell
# 통합 테스트 실행
python -m pytest backend/tests/test_integration_quiz.py -v

# 테스트 시간 단축을 위해 간단한 구조만 확인
python -m pytest -k "integration" -v --tb=short

# ⚠️ 터미널 대화형 입력 방지 (pager 문제)
# 환경변수 설정으로 자동 확장 방지
$env:PAGER='cat'; python -m pytest backend/tests/test_quiz_generator.py -v
```

**터미널 대화형 입력 문제 해결**:
```powershell
# 문제: 터미널이 pager로 이동하여 사용자 입력 대기
# 해결: 환경변수 설정
$env:PAGER='cat'
$env:LESS='-R'

# 또는 pytest 옵션 사용
python -m pytest --disable-warnings --maxfail=1 -r a
```

### 5.3 주요 레슨

**1. Mock 테스트 vs 실제 통합 테스트**
- Mock: 단위 테스트용 (로직 검증)
- 실제 DB: 통합 테스트용 (연결 및 데이터 흐름 검증)
- **결론**: Mock으로만 검증하면 실제 문제를 못 찾음

**2. 학습 플랜 생성 실패 원인**
- Supabase Auth users 테이블에 해당 user_id가 없으면 FK 제약 위반
- **해결**: 테스트용 Auth user 생성 또는 프론트에서 실제 Auth user 사용

**3. 테스트 데이터 준비**
- Phase 1 마이그레이션한 실제 데이터 활용 가능
- books, chapters, summaries 테이블에 데이터 있음
- learning_plans는 실제 Auth user 필요

### 5.4 통합 테스트 패턴

**Fixture 패턴**:
```python
@pytest.fixture
def existing_book_data(supabase_client):
    """기존 book, chapters 데이터 확인"""
    result = supabase_client.table("books").select("id, title, structure_data").limit(1).execute()
    
    if not result.data:
        pytest.skip("테스트 데이터가 없습니다. Phase 1 마이그레이션을 실행해주세요.")
    
    return result.data[0]
```

**테스트 작성 체크리스트**:
- [ ] 실제 Supabase 클라이언트 연결
- [ ] 기존 데이터 활용 (마이그레이션된 데이터)
- [ ] 각 서비스 Layer별 실제 DB 호출 검증
- [ ] 테스트 후 정리 (생성된 데이터 삭제)

---

## 🔗 참고 자료

- [PowerShell 공식 문서](https://docs.microsoft.com/en-us/powershell/)
- [PowerShell vs Bash 문법 비교](https://docs.microsoft.com/en-us/powershell/scripting/learn/ps101/01-getting-started)
- PowerShell 버전 확인: `$PSVersionTable.PSVersion`